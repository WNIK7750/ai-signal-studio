# Collection Domain

使用 `collection.run.start` 采集已启用来源。保留业务 Run ID、成功来源、失败来源、
新增数和去重数。单个来源失败不应丢弃其他来源已经保存的信息。

当计划包含 `web.search.collect` 时，由能力输入中的本地候选数决定是否联网；本地已
达到目标就直接跳过，模型不得为了“更全面”强制重复搜索。联网结果必须先经过安全抓取、
缓存、正常化、去重和标签分析，搜索摘要本身不是最终事实。
