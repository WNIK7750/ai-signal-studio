# ADR-0001：采用前后端分离的模块化单体

状态：Accepted

## 决策

Next.js 与 FastAPI 分离；Python 后端先作为单个部署单元，内部使用模块、Protocol 和 Adapter 隔离。

## 原因

单人维护、需要快速成品，同时希望以后能替换模块。微服务会提前引入部署、网络、鉴权和一致性成本。

## 后果

模块边界通过代码和测试保证，不依赖进程边界。只有出现明确性能或隔离需求时才拆服务。
