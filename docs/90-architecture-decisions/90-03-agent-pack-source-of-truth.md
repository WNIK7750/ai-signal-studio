# ADR-0003：Agent Pack 文档是客制化事实来源

状态：Accepted

## 决策

Agent 身份、行为、偏好、知识、记忆与能力配置使用 Markdown/YAML/JSONL；数据库保存版本和索引元数据。

## 原因

用户与代码 Agent 都能直接修改、导入、审阅和版本控制，避免配置写死在后端。

## 后果

索引必须可重建；导入必须原子；正式记忆修改需要版本记录。
