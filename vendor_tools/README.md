# vendor_tools

本目录隔离所有第三方现成工具、源码 checkout、git submodule、容器定义、补丁和许可证说明。

规则：

- 每个工具独立目录；
- 必须记录上游仓库、版本/commit、许可证和本地修改；
- 自有业务代码不得放在这里；
- 主应用只通过 Adapter/Protocol 调用；
- 升级第三方工具时运行对应契约测试；
- 如果只以外部进程或容器运行，可只保存部署与集成文件，不必复制源码。
